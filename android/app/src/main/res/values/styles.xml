<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema utama aplikasi -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Mengaktifkan mode transparan untuk StatusBar dan NavigationBar -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">#161616</item>
        <item name="android:navigationBarColor">#161616</item>
        
        <!-- Opsional: Membuat ikon StatusBar menjadi gelap jika latar aplikasi Anda putih/terang -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- Opsional: Membuat ikon NavigationBar menjadi gelap jika latar aplikasi Anda putih/terang -->
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>